import { FormatoValorPipe } from './formato-valor.pipe';

describe('FormatoValorPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoValorPipe();
    expect(pipe).toBeTruthy();
  });
});
