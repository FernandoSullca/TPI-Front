import { PorcentajeValorCustomizadoPipe } from './porcentaje-valor-customizado.pipe';

describe('PorcentajeValorCustomizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PorcentajeValorCustomizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
